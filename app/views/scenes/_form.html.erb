<%= form_for(@scene) do |f| %>
  <% if @scene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scene.errors.count, "error") %> prohibited this scene from being saved:</h2>

      <ul>
      <% @scene.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :script_id %><br />
    <%= f.number_field :script_id %>
  </div>
  <div class="field">
    <%= f.label :storyboard_cell_id %><br />
    <%= f.number_field :storyboard_cell_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
